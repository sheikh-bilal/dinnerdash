  <% if obj.errors.any? %>
    <div class="">
      <div class="col-sm-8 col-xs-offset-2 mx-auto">
        <div class="card border-danger mb-3">
          <div class="card-header">
            <h3 class="card-title">
              <%= pluralize(obj.errors.count, "error") %> prohibited this from being saved:
            </h3>
            <div class="card-body">
              <ul>
                <% obj.errors.full_messages.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
